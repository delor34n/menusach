<h1>Local creation</h1>

<form action="{{ path('local_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('local') }}">
            Back to the list
        </a>
    </li>
</ul>
