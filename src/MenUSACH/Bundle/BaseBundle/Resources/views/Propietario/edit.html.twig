{% extends 'MenUSACHBaseBundle:Admin:index.html.twig' %}

{% block body%}
<form class="form-horizontal" action="{{ path('admin_prop_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
    <legend>Editar Propietario</legend>
    {{ form_widget(edit_form) }}
    <div class="controls">
        <input class="btn btn-primary" type="submit" value="Guardar Cambios"/>
    </div>
</form>

<div class="record_actions">
    <a href="{{ path('admin_prop') }}">{{ '.icon-arrow-left Volver a Administración de Propietarios'|parse_icons }}</a>
</div> 
    
{% endblock %}